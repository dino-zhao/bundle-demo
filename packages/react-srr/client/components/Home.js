import React from "react";

const Home = () => {
  return (
    <div>
      <div>Homepage</div>
      <button onClick={() => console.log("click meeee")}>click me</button>
    </div>
  );
};

export default Home;
